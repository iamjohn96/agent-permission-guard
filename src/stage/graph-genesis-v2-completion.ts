/**
 * Reserved completion module boundary.
 *
 * The synthetic V2 completion implementation is deliberately private to
 * ProductionGraphGenesisV2SessionAuthority. This module exports neither a
 * construction input, factory, nor callable completion authority.
 */
export {};
